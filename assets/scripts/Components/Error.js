import React from 'react';

/**
 * Little component which represent base network error.
 * Using in every situation, where network error was thrown.
 */
export default props => <div className="error">Ошибка сети</div>;